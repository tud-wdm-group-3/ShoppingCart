apiVersion: v1
kind: Service
metadata:
  name: order-0
spec:
  type: ClusterIP
  selector:
    component: order-0
  ports:
    - port: 8080
      name: http
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-0
spec:
  replicas: 2
  selector:
    matchLabels:
      component: order-0
  template:
    metadata:
      labels:
        component: order-0
    spec:
      containers:
        - name: order-0
          image: order:latest
          env:
            - name: POSTGRES_USER
              value: pguser
            - name: POSTGRES_PASSWORD
              value: pgpassword
            - name: POSTGRES_DB
              value: pgdb
            - name: POSTGRES_HOST
              value: order-0-db-postgresql-ha-pgpool
            - name: SERVICE_PORT
              value: "8080"
          imagePullPolicy: Always
          resources:
            limits:
              memory: "1Gi"
              cpu: "1"
            requests:
              memory: "1Gi"
              cpu: "1"
          command: ["java"]
          args: ["-jar", "/srv/order/target/order-1.0-SNAPSHOT.jar"]
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: order-1
spec:
  type: ClusterIP
  selector:
    component: order-1
  ports:
    - port: 8080
      name: http
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-1
spec:
  replicas: 2
  selector:
    matchLabels:
      component: order-1
  template:
    metadata:
      labels:
        component: order-1
    spec:
      containers:
        - name: order-1
          image: order:latest
          env:
            - name: POSTGRES_USER
              value: pguser
            - name: POSTGRES_PASSWORD
              value: pgpassword
            - name: POSTGRES_DB
              value: pgdb
            - name: POSTGRES_HOST
              value: order-1-db-postgresql-ha-pgpool
            - name: SERVICE_PORT
              value: "8080"
          imagePullPolicy: Always
          resources:
            limits:
              memory: "1Gi"
              cpu: "1"
            requests:
              memory: "1Gi"
              cpu: "1"
          command: ["java"]
          args: ["-jar", "/srv/order/target/order-1.0-SNAPSHOT.jar"]
          ports:
            - containerPort: 8080
